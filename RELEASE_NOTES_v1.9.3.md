# StockDesk v1.9.3

## 行情与数据层

- **北交所历史行情修复**：拒绝单根占位数据，自动清理旧的不完整缓存，并新增**新浪日线独立容灾源**，避免北交所个股只有一根 K 线时被当成有效历史。
- **日 K 统一口径**：主进程统一请求 750 根日 K，供图表、信号、策略和研报共同复用，减少重复请求。
- **研报行情逐级回退**：研报所需行情不再依赖单一快照，改为「Provider Hub → 多源报价 → 最近日线」逐级回退。
- **资金流解析加固**：`parseCapitalFlowLine` 对 `-` 占位、字段缺失与格式异常返回 `null`，`summarizeCapitalFlow` 明确区分 `ok / partial / unavailable`，**数据不可用时不作为资金方向判断依据**。

## 选股

- 行业板块在**数据层与渲染层按规范化名称、代码双重去重**，消除「同一行业出现两次」与 `（undefined）` 计数问题。
- `dedupeIndustryBoards` / `matchIndustryBoard` 收敛到数据层统一实现，避免各调用点口径不一致。

## 信号

- 新增 **`engine/trend-horizons.js`**：短期（5/10）、中期（20/60）、长期（60/120）趋势**各自独立计算**，只描述当前形态，不输出未来收益预测。
- 新增 **`engine/signal-service.js`**：统一信号计算与缓存入口，主进程与离线回归共用同一实现；刷新时可绕过日 K / 分时缓存。
- 信号面板补充数据完整度、历史可靠度与多周期方向，并明确标注「日 K 未包含今日时，趋势仅代表历史数据」。

## 研报

- 按股票代码**精确采集法定披露公告**，并分为「公司治理 / 经营事项」与「财报业绩」两组展示，减少无关公告噪声。
- 公告与新闻摘要做公司相关性过滤（`filterCompanySnippets`），剔除聚合页与他人公司内容。

## 稳定性

- **切股同步修复**：切换股票时同步刷新信号、策略和综合研报，并**隔离过期异步响应**，避免上一只股票的结果覆盖当前面板。
- 数据源调度补充 `push2delay` 等备用域名回退，缓解部分网络环境下东财行情域名被链路重置的问题。

## 功能范围调整

- **移除了多投资人评审团（深度分析）与多股对比面板**，同时移除其依赖的规则引擎、机构估值模型与游资席位数据。
- 相应移除的 IPC 通道与 `preload` 暴露面在 `scripts/ui-contract-check.mjs` 中加了**回归断言**，防止误引入；`scripts/unit-check.mjs` 断言研报中不再出现评审团相关字段。
- 保留并独立出来的通用能力：`engine/company-search.js`（企业公告 / 公司新闻检索）、`engine/market-fetchers.js`（东财 F10 / 龙虎榜 / 研报 / 分红 / 资金流）。

## 测试

- 新增 `scripts/capital-flow-check.mjs`、`scripts/signal-trends-check.mjs`。
- 发布门禁覆盖 **16 项**离线检查，全部通过。
